///A Favorate Color
enum Color {
    Red,
    Blue,
}

///A piece of mail.
struct Mail {
    address: String,
}

///adds two numbers together
fn add(a: i32 , b: i32) -> i32 {
    a + b
}
fn main() {}

//use cargo doc --open "for opening program docs"
//use rustup doc for opening rust docs